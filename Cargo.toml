[package]
name = "macron"
description = "Comprehensive Rust macros toolkit for everyday development tasks. Includes convenient string formatting (str!), regex pattern matching (re!), streamlined collection creation, and custom derive macros for Display, Error, From, and Into traits. Boost your productivity with intuitive syntax and simplify your codebase while maintaining performance and safety. Perfect for both small projects and large-scale applications."
keywords = ["macros", "macro", "proc", "derive", "declarative"]
categories = ["development-tools", "rust-patterns"]
version = "0.1.18"
license = "MIT"
authors = ["Bulat Sh. <synapdrake@ya.ru>"]
repository = "https://github.com/fuderis/macron-rs"
readme = "README.md"
edition = "2021"

[workspace]
members = ["crates/collections", "crates/impl-display", "crates/impl-error", "crates/impl-from", "crates/impl-into", "crates/path", "crates/str", "crates/map", "crates/regex", "crates/input", "crates/inputln"]
default-members = ["crates/collections", "crates/impl-display", "crates/impl-error", "crates/impl-from", "crates/impl-into", "crates/path", "crates/str", "crates/map", "crates/regex", "crates/input", "crates/inputln"]
resolver = "3"

[workspace.dependencies]
proc-macro2 = "1.0.95"
quote = "1.0.39"
syn = { version = "2.0.100", features = ["parsing"] }
darling = "0.23.0"

[dependencies]
macron-path = { version = "0.1.3", optional = true }
macron-collections = { version = "0.1.6", optional = true }
macron-impl-display = { version = "0.2.0", optional = true }
macron-impl-error = { version = "0.1.3", optional = true }
macron-impl-from = { version = "0.2.0", optional = true }
macron-impl-into = { version = "0.2.0", optional = true }
macron-regex = { version = "0.1.1", optional = true }
macron-str = { version = "0.1.1", optional = true }
macron-input = { version = "0.1.0", optional = true }
macron-inputln = { version = "0.1.1", optional = true }
async-recursion = { version = "1.1.1", optional = true }

[features]
full = ["path", "string", "input", "collections", "derive", "async-recursion"]
path = ["dep:macron-path"]
string = ["dep:macron-str", "dep:macron-regex"]
input = ["dep:macron-input", "dep:macron-inputln"]
collections = ["dep:macron-collections"]
derive = ["dep:macron-impl-display", "dep:macron-impl-error", "dep:macron-impl-from", "dep:macron-impl-into"]
async-recursion = ["dep:async-recursion"]
